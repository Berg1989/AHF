<div class="col">
    <div class="row">
        <h3>Profil tilhørende: {{user.firstname}} {{user.lastname}}</h3>
    </div>
    <div class="row">
        <h3>Email: {{user.email}}</h3>
        </div>
        <form method="POST" action="/profile/logout">
            <button type="submit" name="logout">Logout</button>
        </form>
    </div>
</div>