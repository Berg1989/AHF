<div class="row">
    <div class="col-md-3">
        {{> shopNav}}
    </div>
    <div class="col-md-9">
        {{#if messages.errors }}
        <div class="alert alert-danger">
            {{#each messages.errors }}
            <span>{{ this.msg }}</span><br>
            {{/each}}
        </div>
        <hr>
        {{/if}}
        {{#if messages.success }}
        <p class="alert alert-success">{{ messages.success }}</p>
        <hr>
        {{/if}}
        <h1>Velkommen til shoppen</h1>
        <p>Du kan vælge en produktkategori i menuen til venstre</p>
    </div>
</div>