<div class="row">
    <div class="col">
        <div class="row-lg-6">
            <div class="card">
                <div class="card-header">
                    <h1>Nyheder</h1>
                </div>
            {{#each posts }}
                <div class="card-body border border-light">
                    <h5 class="card-title">{{this.headline}}</h5>
                    <p class="card-text"><small><small>Opslået: {{this.postdate}}</small></small></p>
                    <p class="card-text">{{this.body}}</p>
                    <p class="card-text"><small><small>Af: {{this.author}}</small></small></p>
                    <a href="/posts/{{this.id}}" class="btn btn-primary">Til opslag</a>
                </div>
            {{/each}}
            </div>
        </div>
    </div>
</div>