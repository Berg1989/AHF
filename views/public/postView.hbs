<script>
    function updateTimeView() {
        const postdateinput = document.getElementById("postdate");


        const postdatedate = new Date(postdateinput.innerHTML).toDateString();

        deadlineinput.innerHTML = '<strong>Dato:</strong> ' + deadlinedate;
    }
</script>

<div class="row">
    <div class="col">
        {{#if messages.errors }}
        <div class="alert alert-danger">
            {{#each messages.errors }}
            <span>{{ this.msg }}</span><br>
            {{/each}}
        </div>
        <hr>
        {{/if}}
        {{#if messages.success }}
        <p class="alert alert-success">{{ messages.success }}</p>
        <hr>
        {{/if}}
    </div>
</div>
<div class="row">
    {{!-- event information --}}
    <div class="col-lg-12">
        <div class="row">
            <h1>{{posts.headline}}</h1>
        </div>
        <div class="row">
            <p id="postdate">{{posts.postdate}}</p>
        </div>
        <div class="row">
            <p><strong>af</strong> {{posts.author}}</p>
        </div>
        <div class="row">
            <p><strong>Opslag:</strong></p>
        </div>
        <div class="row">
            <p class="col-lg-6" readonly>{{posts.body}}</p>
        </div>
    </div>

</div>

<script>
    updateTimeView(); 
</script>